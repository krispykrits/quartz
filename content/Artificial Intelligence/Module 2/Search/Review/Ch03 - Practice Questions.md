# Practice Questions

1. Define a search problem for navigating a warehouse robot using all five components.
2. Why is “Arad” an abstraction rather than a complete physical state?
3. Distinguish a state from a search node.
4. Explain why cycles make a search tree infinite over a finite graph.
5. Trace BFS on a graph with branching factor 2 and a goal at depth 3.
6. Why is BFS's early goal test valid under equal costs?
7. Construct a graph where BFS returns a more expensive path than UCS.
8. Trace UCS and show when a reached entry must be replaced.
9. Compare DFS, depth-limited search, and iterative deepening.
10. What must be true for bidirectional search to be practical?
11. For nodes A $(g=5,h=8)$ and B $(g=9,h=3)$, which do UCS, greedy, and A* expand first?
12. Read aloud and explain $h(n)\le c(n,a,n')+h(n')$.
13. Prove that consistency makes $f$ nondecreasing.
14. Reproduce the A* optimality contradiction using $C$ and $C^*$.
15. Interpret an $f=400$ contour.
16. True or false: an optimally efficient algorithm must run in polynomial time. Explain.
17. Explain how weighted A* changes behavior as $W$ rises.
18. Compare IDA* and RBFS.
19. Define effective branching factor and explain what a decrease means.
20. Why does Manhattan distance dominate misplaced tiles for the 8-puzzle?
21. Derive an admissible heuristic from a relaxed problem of your choice.
22. Prove the maximum of admissible heuristics is admissible.
23. Explain why summing two pattern databases can double-count.
24. Interpret a pattern-database diagram with starred/ignored tiles.
25. Explain how landmark distances yield a lower bound.
26. Long answer: choose an algorithm for a huge weighted graph with little memory and justify every tradeoff.

